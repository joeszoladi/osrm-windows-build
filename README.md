Windows app for Select Classic Carriers

App built in WPF, includes OSRM v6 and Canada map set.

build-canada-osrm-v6.yml is OSRM v6 map set, build-osrm-v6-windows.yml is OSRM v6 runtime

included will be the powershell script to join both together into a functioning windows app. the only thing missing is the mysql server and db.
will attempt to add that into the app as well at some point so it is a completely self contained app that can be installed and used on any windows computer

Joe Szoladi
